language: python

python: 2.7

env:

script: true
archive: true

before_install:
  - pypy --version
  - sudo add-apt-repository ppa:pypy/ppa
  - sudo apt-get update
  - sudo apt-cache showpkg pypy
  - sudo apt-get install pypy=1.8+dfsg-2
install:
  - pypy --version
before_script:

script:

after_script:

