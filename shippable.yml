language: php 

php: 5.6

script: true
archive: true

before_install:
  
install:
  - psql -U postgres -c "create USER dbuser WITH PASSWORD 'dbpassword' SUPERUSER;"
  - echo dbpassword | psql -d mydb -U myuser -W

before_script:

script:

after_script:

