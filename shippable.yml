language: node_js

node_js:
  - 0.12
  before_install:
    - source ~/.rvm/scripts/rvm
    - rvm use 1.9.3
    - source ~/.bashrc && ~/.rvm/scripts/rvm && rvm use 1.9.3
    
script: true


install:
  
before_script:

script:

after_script:


