language: python

script: true

before_install:
  - npm -v
  - npm install -g npm@2.12.1
  - npm -v
  - npm install gulp-imagemin

before_script:
  - npm -v

script:
  - npm -v

after_script:
  - npm -v
