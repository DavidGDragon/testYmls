language: python

python: 2.7

env:

script: true
archive: true

before_install:
  - pypy --version
  - sudo add-apt-repository ppa:pypy/ppa
  - sudo apt-get update
  - sudo apt-cache showpkg pypy
  - sudo apt-get -t="2.6.1+dfsg-2~ppa0~ubuntu14.04" install pypy
install:
  - pypy --version
before_script:

script:

after_script:

