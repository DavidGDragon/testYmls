language: python

python: 2.7

script: true
archive: true

before_install:
  
install:
  - curl -s checkip.dyndns.org | sed -e 's/.*Current IP Address: //' -e 's/<.*$//'  

before_script:

script:
 - sleep 300

after_script:

