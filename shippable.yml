language: php 

php: 5.6

script: true
archive: true

before_install:
  
install:
  - psql -U postgres -c "create USER dbuser WITH PASSWORD 'dbpassword' SUPERUSER;"
  - psql -d mydb -U myuser -W | echo dbpassword

before_script:

script:

after_script:

