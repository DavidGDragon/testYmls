language: node_js

node_js:
  - 0.12.7
  - 0.12.2
  
services:
  - redis
  - mysql
  
script: true
archive: true

before_install:
  
install:
  - echo 'install'

before_script:

script:

after_script:

