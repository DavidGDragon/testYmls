language: php 

php: 5.6

script: true
archive: true

before_install:
  - wget http://apache.mirrors.hoobly.com//httpd/httpd-2.4.16.tar.gz
  - gzip -d httpd-2.4.16.tar.gz
  - tar xvf httpd-2.4.16.tar
  - cd httpd-2.4.16
  - ./configure --prefix=$(pwd)
  - make
  - make install
  
install:
  - httpd -v

before_script:

script:

after_script:

