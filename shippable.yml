language: php 

php:
  - 5.5

env:
  matrix:
    - MAGENTO_VERSION=1.9.1.1

script: true
archive: true

before_install:
  - ls
  - echo $MAGENTO_VERSION
  - . instal_magneto
  
install:
  - echo true

before_script:

script:

after_script:

